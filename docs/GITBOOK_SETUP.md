# GitBook Setup (EverDraw)

1. In GitBook, connect GitHub repo: `Gmankey/everdraw`
2. Content root: `docs/`
3. Default branch: `main`
4. Ensure `SUMMARY.md` is used for navigation
5. Enable auto-sync from GitHub

## Pre-publish placeholders
- `0x[MAINNET ADDRESS]` in `developers/smart-contract.md`, `security.md`
- `[Contact →](#)` in `for-protocols.md`
- MonadVision explorer URL in `security.md`
