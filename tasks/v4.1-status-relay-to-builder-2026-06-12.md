# Relay to builder — V4.1 ops state + open items (2026-06-12)

Catch-up after the treasury/alerting work. Source of truth is `origin/staging` (`Gmankey/everdraw`); `everdraw-clean` is reference-only. Docs referenced below live under `tasks/` on the `design/v5-m0-amendments` branch.

## What's confirmed done (verified live, not just merged)
- **V4.1-B deployed + recorded.** Vault `0x1886f329e486e934c76028B15a580850e74d404C`, oracle `0xd5d43554CA158334d5Db4aEA745Ead986fAad5C5`. Recorded in canonical `deployments/monad-mainnet.json` (PR #99) + frontend cutover (PR #100). Stagger landed inside the guard window.
- **Fly keeper secrets repointed + verified.** `everdraw-keeper` `POOL_ADDRESSES` = `POOL_ADDRESSES_V3` = V4.1-A, V4-B, V4.1-B, retired V4-A. `printenv` confirms; keeper logs show it ticking all four live vaults. The June 7→12 blind window (keeper was watching only dead vaults) is closed.
- **V4.1-A VRF reserve seeded (9 MON, Ledger).** It was deployed with zero reserve — round 1 had stalled and nothing alarmed. Now fixed.

## Open items — please action / confirm

1. **VERIFY ON ~2026-06-14T15:00Z: V4.1-A round 1 commits + settles end-to-end.** Right now it's round 1 / Open, reserve present, `nextExecutable = None` (correct — yield window not elapsed). Commit comes due ~06-14 15:00Z; the keeper is now watching it so it should fire automatically. Watch the VRF request → settle on-chain and confirm.
2. **Merkl resubmission on the V4.1 pair** — now that V4.1-B is live and the frontend is cut over. Single submission on V4.1-A + V4.1-B addresses.
3. **Alerting hardening** — `tasks/alerting-audit-incident-2026-06-12.md` is the ticket (10 gaps). Priority: dead-man heartbeats on keeper + alerts via an independent channel (healthchecks.io, not Telegram) this week; then G1 canonical-pool-list reconciliation (read from `origin/staging`), G3 re-alert backoff, G5 round-overdue alert, G6 next-commit-fee projection. Indexer has zero alerting (G9).
4. **V4-A retired close-out** — gated on the 0.638 shMON depositor balance being withdrawn; then Ledger `withdrawVRFReserve` (8.23, record says 9 — fix the stale figure) + `stop()`.
5. **V4-B retirement** — gated on V4.1-B cutover being verified live; don't pull forward.

## New standing rule — applies to you too
**Neither PM nor builder ever creates, funds, holds, or sweeps a wallet/private key.** Two self-managed throwaway deployer keys lost funds (V4.1-A ~2.41 MON stranded, V4.1-B ~0.098 dust). Going forward, any signing-key step = **operator creates/nominates a wallet, funds it, retains the key.** The `.openclaw/secrets/` save-then-delete pattern in `tasks/v4.1-deploy-plan.md` is retired. Carry this into the V5 deploy/ops runbooks.

## Not your action, FYI
Burner test wallet `0x69b3F8…9EBc` (10.31 MON) had its key in plaintext bash history — redacted; operator keeps the wallet. Two deployer keys (~2.51 MON total) written off as unrecoverable.

## Sequencing unchanged
V4.1 items above stay ahead of V5 build (M1+). M0 is signed off (PR #97); V5 implementation does not start until the V4.1 track is clear.
