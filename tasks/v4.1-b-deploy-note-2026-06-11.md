# V4.1-B Deploy — go now (2026-06-11)

**Implements:** `tasks/v4.1-deploy-plan.md` step 4 (V4.1-B at +3.5 days), per the ADR-0010/0033 stagger guard.

## Timing
- V4.1-A anchor (round-1 sales-end): `2026-06-07T15:03:17Z`
- +3.5 days target: `2026-06-11T03:03:17Z`
- Stagger guard tolerance: ±12h → window closes `2026-06-11T15:03:17Z`
- It is currently `2026-06-11T14:55Z` — **inside the window, ~8 minutes of margin.** Deploy now; do not wait for next cycle (the guard will hard-abort past 15:03:17Z without `STAGGER_OVERRIDE=1`, which we do not want to use).

## Action
Execute `tasks/v4.1-deploy-plan.md` step 4 — deploy V4.1-B:
- `VAULT_SYMBOL="EVRDRAW-B"`
- `STAGGER_REFERENCE_VAULT=0x933FF608eaC2b3221088bd9AE19b05F266dBF7DA` (V4.1-A)
- All other env identical to V4.1-A (VERSION 4.1.0, native MON deposit mode, shMON yield vault `0x1B68626dCa36c7fE922fD2d55E4f631d962dE19c`, 1 MON ticket, 24h round, 518100s yield period, 1 winner / 10000 bps, ADR-0029 Pyth oracle pattern)

## Checklist (from deploy plan)
1. Operator funds fresh deployer (~11 MON), saved to `.openclaw/secrets/`.
2. Deploy V4.1-B with the stagger guard env above — confirm it does **not** abort.
3. Seed 9 MON VRF reserve, `setKeeper(Fly,true)` + de-authorize deployer, `setPauser(Ledger)`, `transferOwnership(Ledger)`.
4. Operator `acceptOwnership` on Ledger (verify address on-device).
5. Sourcify-verify pool + oracle.
6. On-chain smoke checks per `tasks/v4.1-deploy-plan.md` ("Verify on-chain" block): `VERSION()` = "4.1.0", `getRoundTicketPrice` = 1e18, shMON deposit path.
7. Sweep + delete the deploy-only key **only after** sweep tx confirmed and balance read back on-chain — never combined with deletion in one script.

## After deploy
- Update `deployments/monad-mainnet.json` (new V4.1-B entry: address/deployTx/block/anchor/oracle/bytecode-hash/verification) and ADR-0032 with the actual anchor + addresses. Confirm the A→B anchor gap is ~3.5 days.
- Then proceed to cutover: frontend `VITE_POOL_ADDRESSES` slot B → V4.1-B, keeper/indexer Fly secrets (no `--stage`), Sourcify verification, then single Merkl submission on the V4.1 pair once both are live and indexed.
- `stop()` the old retired Vault B (V4) (`0x08bdD3710abB0616Cc29f388867f5625106B2A3E`) per the runbook once shMON deposits land on V4.1-B and depositors have had a chance to withdraw.

## Out of scope here
V5 work does not start until M0 sequencing items (this deploy, shMON frontend cutover, Merkl resubmission) are clear — see `tasks/v5-handoff-message-to-mendel.md`.
