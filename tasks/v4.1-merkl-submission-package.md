# V4.1 Merkl submission package (ready to send)

**Date:** 2026-06-17. **Owner of the send:** operator (external coordination with Merkl/shMonad). PM prepared; verified on-chain 2026-06-17.
**Cites:** ADR-0006 (Merkl surface), ADR-0035 (single Merkl submission on V4.1 — do NOT submit the old MON-only addresses).

## Submit these (and only these)

| Vault | Address | Chain |
|---|---|---|
| EverDraw Vault A (V4.1-A) | `0x933FF608eaC2b3221088bd9AE19b05F266dBF7DA` | Monad mainnet (chainId **143**) |
| EverDraw Vault B (V4.1-B) | `0x1886f329e486e934c76028B15a580850e74d404C` | Monad mainnet (chainId **143**) |

Both must be registered (per-vault balances summed off-chain by Merkl). **Do not** submit any prior V2/V3/V4 address.

## Surface Merkl needs (verified live on-chain 2026-06-17)
- `totalSharesFunctionSignature` → `totalSupply()` → uint256
- `userSharesFunctionSignature` → `balanceOf(address)` → uint256 (returns **MON-principal**, 1 MON = 1e18; stable, not shMON shares)
- Events: `Deposit(address indexed recipient, uint256 amount)`, `Withdraw(address indexed recipient, uint256 amount)` — Merkl's event-based TWAB model (they do not poll balances)
- `decimals()` = 18. Positions are non-transferable (no transfer/approve by design).
- Verified: VERSION 4.1.0; A `totalSupply` 3.0 MON, B 0.0 (live).

## Paste-ready message to Merkl/shMonad

> Hi — EverDraw is live on Monad mainnet (chainId 143) on our final V4.1 vault pair. Please index these two for shMonad points (event-based, per your generic vault integrator):
>
> - Vault A: `0x933FF608eaC2b3221088bd9AE19b05F266dBF7DA`
> - Vault B: `0x1886f329e486e934c76028B15a580850e74d404C`
>
> Total shares: `totalSupply()`. User shares: `balanceOf(address)` (returns MON-principal, 18 decimals). Stake events: `Deposit(address indexed recipient, uint256 amount)` and `Withdraw(address indexed recipient, uint256 amount)`. Please disregard any earlier EverDraw addresses — these V4.1 pair are canonical.

## Note for V5
When V5 ships (continuous TWAB), Merkl registration moves to the V5 addresses at M9 cutover. This V4.1 submission stands until then.
